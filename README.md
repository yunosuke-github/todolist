# React Todo List

This repository contains a minimal todo list implemented using React. It uses CDN versions of React and ReactDOM so no build steps are required.

## Usage

Open `index.html` in a web browser to use the app. You can add tasks, mark them complete and delete them.
